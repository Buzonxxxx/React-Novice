import React from 'react';

//output the length of entered text
const App = props => {
  return (
    <div>
      .length()

      </div>
  )
}
//receives text length as prop
const Validation = props =>{
{textLength > 5  ? 'Text too short' : 'Text long enough'}
}

const Char = props =>{
  
}
